# Phase 1 only: microphone capture, silence detection, speech-to-text.
# No interface libraries yet - those arrive in Phase 3.

faster-whisper>=1.0.3     # speech to text (CTranslate2 build of Whisper)
sounddevice>=0.4.6        # microphone capture
numpy>=1.24               # audio buffers
webrtcvad-wheels>=2.0.14  # voice activity detection (prebuilt; no compiler needed)
